{
  "name": "avemguvern-ro",
  "version": "1.0.0",
  "private": true,
  "description": "Does Romania have a government? A one-question site + API + MCP server on Cloudflare Workers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "seed": "wrangler kv key put --binding=GOV_STATUS current \"$(cat seed.json)\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0",
    "@cloudflare/workers-types": "^4.20240000.0"
  }
}
